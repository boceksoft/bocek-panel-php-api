<?php

declare(strict_types=1);

/*
 * Sunucuya özel ayar dosyası ŞABLONU. Bu dosyayı sunucuda
 * "app.local.php" adıyla kopyala ve gerçek değerleri gir.
 *
 * app.local.php GİT'E GİRMEZ (.gitignore'da) ve otomatik güncelleme
 * (backend-api/update) BU DOSYAYA DOKUNMAZ — böylece her sitenin kendi
 * sırları ve site-özel ayarları (base_path, allowed_ips vb.) güncelleme
 * sırasında ASLA ezilmez.
 *
 * config/app.php (repo'daki, güncellemede üzerine yazılan) ile bu dosya
 * bootstrap.php içinde birleştirilir; buradaki değerler önceliklidir.
 */

return [
    // Otomatik güncelleme (backend-api/update) için paylaşılan sır.
    // Rastgele, uzun bir değer üret: bin2hex(random_bytes(24))
    'deploy_secret' => 'Deneme123',

    // GitHub private repo ise: sadece bu repoya "Contents: Read-only"
    // izniyle bir Fine-grained Personal Access Token.
    'github_token' => '',

    // Bu sitede base_path farklıysa burada override et (örn. kök dizinde ise '').
    // 'base_path' => '/backend-api',

    // Bu sitede dolu tablosundaki kayit iliski kolonu farkliysa override et.
    // Ornekler: 'kayitId', 'kayitid'
    // 'dolu_kayit_id_column' => 'kayitid',

    // Bu sitede ruleshomes tablosundaki rule iliski kolonu farkliysa override et.
    // Ornekler: 'rulesId', 'rulesid'
    // 'ruleshomes_rules_id_column' => 'rulesId',

    // Bu sitede ruleshomes tablosundaki home iliski kolonu farkliysa override et.
    // Ornekler: 'homesId', 'homesid'
    // 'ruleshomes_homes_id_column' => 'homesId',

    // Bu sitede rulesruletypes tablosundaki rule iliski kolonu farkliysa override et.
    // Ornekler: 'rulesId', 'rulesid'
    // 'rulesruletypes_rules_id_column' => 'rulesId',

    // Bu sitede redirects tablosunda expiredDate/expiredMode kolonlari yoksa false yap.
    // 'links_use_expiration_columns' => true,

    // Link olustururken redirect hedefinde kullanilan arama sayfasi sorgusu.
    // Sorgu ilk satirdaki "url" alanindan arama sayfasi yolunu okumalidir.
    // 'links_search_page_query' => 'SELECT url FROM tip WHERE id = 1',

    // Bu sitede acenta_users tablosu yoksa false yap.
    // 'reservation_filters_use_acenta_users' => false,

    // Bu sitede kayitlar.maliyet kolonu yoksa false yap.
    // 'reservations_use_maliyet_column' => false,

    // Bu sitede defter.rezid farkliysa override et.
    // Ornekler: 'rezid', 'rezId', 'kayitid', 'kayitId'
    // 'defter_reservation_id_column' => 'rezid',

    // Bu sitede kayitlar.ulke farkliysa override et.
    // Ornekler: 'ulke', 'ulkeKodu', 'countryCode'
    // 'reservations_country_code_column' => 'ulke',

    // Kolon tanimi setup endpoint'i farkli tabloya yazacaksa override et.
    // Ornekler: 'kolonlar', 'dbo.kolonlar'
    // 'kolonlar_table' => 'kolonlar',

    // Iptal sarti select'i icin sayfalar{dbtable}.id degeri farkliysa override et.
    // Tek deger: 'homes_management_cancellation_policy_page_id' => 4,
    // Site bazli: 'homes_management_cancellation_policy_page_id' => [1 => 4, 2 => 12],

    // Rezervasyon ozeti HTML'ini PNG/JPG'ye cevirmek icin wkhtmltoimage yolu.
    // Proje ici Windows:
    // 'html_to_image_binary' => __DIR__ . '/../bin/wkhtmltoimage.exe',
    // Proje ici Linux:
    // 'html_to_image_binary' => __DIR__ . '/../bin/wkhtmltoimage',
    // Windows:
    // 'html_to_image_binary' => 'C:/Program Files/wkhtmltopdf/bin/wkhtmltoimage.exe',
    // Linux:
    // 'html_to_image_binary' => '/usr/bin/wkhtmltoimage',

    // Site kolon suffix yapisi farkliysa override et.
    // Ornek: site 2 genel olarak _s2 kolonlarini kullaniyorsa:
    // 'site_column_suffixes' => [1 => '', 2 => '_s2', 3 => '_s3'],

    // Tekil kolonlar farkli davranacaksa override et.
    // Ornek: site 2 depozito_s2 yoksa ortak depozito kolonunu kullan:
    // 'site_field_suffixes' => [2 => ['depozito' => '']],

    // Bu sitede farklı IP kısıtı gerekiyorsa override et.
    // 'allowed_ips' => ['1.2.3.4'],

    // Güncelleme sırasında zip'in açıldığı geçici klasör. Varsayılan
    // (config/app.php) backend-api'nin BİR ÜSTÜNDEKİ uploads/git-temp'tir.
    // Bu sitede uploads/ farklı bir yerdeyse (örn. ayrı bir cdn subdomain'i
    // altında) burada TAM yolu ver. Örnek — backend-api iki üstteki
    // cdn.villakilavuzu.com/uploads/git-temp'i kullanmalıysa:
    // 'git_temp_dir' => dirname(__DIR__, 3) . '/cdn.villakilavuzu.com/uploads/git-temp',
];
